import { GraphPanel } from "@/components/graph/GraphPanel";

export default function ExplorerPage() {
  return (
    <main className="w-screen h-screen m-0 p-0 overflow-hidden bg-[#111111]">
      <GraphPanel />
    </main>
  );
}
